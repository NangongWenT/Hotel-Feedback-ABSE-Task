# 🚀 启动说明

## ✅ 当前状态

- ✅ Node.js v24.11.0 已安装
- ✅ npm 11.6.1 可用
- ✅ 前端依赖已安装
- ✅ 前端开发服务器已启动

## 📝 重要提示：刷新环境变量

**如果新开 PowerShell 窗口后 `node` 或 `npm` 命令找不到，运行以下命令刷新环境变量：**

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

**或者运行快速修复脚本：**

```powershell
.\快速修复.ps1
```

**或者最简单的方法：关闭并重新打开 PowerShell 窗口。**

---

## 🎯 完整启动流程

### 终端 1：启动后端

```powershell
cd "C:\Users\32353\Desktop\大四上\NLP\CW\Hotel-Feedback-ABSA-Task-main\backend"
.\venv\Scripts\python.exe app.py
```

**成功标志：**
```
✅ 检测到CUDA，将使用GPU: NVIDIA GeForce RTX 5060 Laptop GPU
 * Running on http://127.0.0.1:5000
```

### 终端 2：启动前端

**如果新开 PowerShell 窗口，先刷新环境变量：**

```powershell
# 刷新环境变量（如果 node 命令找不到）
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

# 进入前端目录
cd "C:\Users\32353\Desktop\大四上\NLP\CW\Hotel-Feedback-ABSA-Task-main\frontend"

# 启动前端（依赖已安装，直接启动即可）
npm run dev
```

**成功标志：**
```
  VITE v5.x.x  ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

---

## 🌐 访问系统

1. 打开浏览器
2. 访问：**http://localhost:5173**
3. 使用默认管理员账户登录：
   - 用户名：`admin`
   - 密码：`admin123`

---

## ⚠️ 常见问题

### Q1: 新开 PowerShell 窗口后 node 命令找不到？

**解决方案：**

**方法 1：刷新环境变量（推荐）**
```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

**方法 2：运行快速修复脚本**
```powershell
.\快速修复.ps1
```

**方法 3：关闭并重新打开 PowerShell**

**方法 4：重启电脑（如果以上方法都不行）**

### Q2: 前端服务器无法访问？

**检查：**
1. 确保后端已启动（http://localhost:5000）
2. 确保前端已启动（http://localhost:5173）
3. 检查防火墙设置
4. 查看终端中的错误信息

### Q3: 端口被占用？

**解决方案：**
- 后端端口 5000 被占用：修改 `backend/app.py` 中的端口
- 前端端口 5173 被占用：修改 `frontend/vite.config.js` 中的端口

---

## 📋 快速命令参考

### 刷新环境变量
```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

### 验证 Node.js
```powershell
node --version
npm --version
```

### 启动后端
```powershell
cd backend
.\venv\Scripts\python.exe app.py
```

### 启动前端
```powershell
cd frontend
npm run dev
```

---

## 🎉 完成！

现在你的系统应该可以正常运行了！

- ✅ 后端：http://localhost:5000
- ✅ 前端：http://localhost:5173
- ✅ GPU 加速：已启用

享受使用吧！🚀

