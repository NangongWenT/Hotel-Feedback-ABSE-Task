# 上传代码到 GitHub 指南

## 📋 前提条件

### 步骤 1：安装 Git

1. **下载 Git for Windows**
   - 访问：https://git-scm.com/download/win
   - 下载并安装（使用默认设置即可）

2. **验证安装**
   ```powershell
   git --version
   ```
   应该显示类似：`git version 2.x.x`

3. **配置 Git（首次使用）**
   ```powershell
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```

## 🚀 上传代码到 GitHub

### 方法 1：创建新分支（推荐）

适合：保留原版本，在新分支上继续开发

```powershell
# 1. 进入项目目录
cd "C:\Users\32353\Desktop\大四上\NLP\CW\Hotel-Feedback-ABSA-Task-main"

# 2. 初始化 Git 仓库（如果还没有）
git init

# 3. 添加远程仓库（替换为你的 GitHub 仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 4. 添加所有文件
git add .

# 5. 提交更改
git commit -m "修复编码问题和登录功能，优化情感分析"

# 6. 创建新分支（例如 v2.0）
git checkout -b v2.0

# 7. 推送到 GitHub
git push -u origin v2.0
```

### 方法 2：创建新标签（版本发布）

适合：标记特定版本，保留主分支不变

```powershell
# 1-5. 同方法 1

# 6. 创建标签（例如 v2.0）
git tag -a v2.0 -m "版本 2.0: 修复编码问题和登录功能"

# 7. 推送代码和标签
git push origin main
git push origin v2.0
```

### 方法 3：创建新仓库（完全独立）

适合：想要一个全新的仓库

1. **在 GitHub 上创建新仓库**
   - 访问：https://github.com/new
   - 创建新仓库（例如：`Hotel-Feedback-ABSA-Task-v2`）

2. **上传代码**
   ```powershell
   cd "C:\Users\32353\Desktop\大四上\NLP\CW\Hotel-Feedback-ABSA-Task-main"
   git init
   git add .
   git commit -m "版本 2.0: 修复编码问题和登录功能"
   git branch -M main
   git remote add origin https://github.com/你的用户名/新仓库名.git
   git push -u origin main
   ```

## 📝 详细步骤（如果已有 GitHub 仓库）

### 步骤 1：检查当前状态

```powershell
cd "C:\Users\32353\Desktop\大四上\NLP\CW\Hotel-Feedback-ABSA-Task-main"
git status
```

### 步骤 2：查看远程仓库

```powershell
git remote -v
```

如果没有远程仓库，需要添加：
```powershell
git remote add origin https://github.com/你的用户名/你的仓库名.git
```

### 步骤 3：创建新版本

**选项 A：创建新分支**
```powershell
git checkout -b v2.0
git add .
git commit -m "版本 2.0: 修复编码问题和登录功能"
git push -u origin v2.0
```

**选项 B：创建标签**
```powershell
git add .
git commit -m "版本 2.0: 修复编码问题和登录功能"
git tag -a v2.0 -m "版本 2.0: 修复编码问题和登录功能"
git push origin main
git push origin v2.0
```

## 🔍 常见问题

### Q1: 如何查看所有分支？

```powershell
git branch -a
```

### Q2: 如何切换分支？

```powershell
git checkout main  # 切换到主分支
git checkout v2.0  # 切换到 v2.0 分支
```

### Q3: 如何查看所有标签？

```powershell
git tag
```

### Q4: 如何删除分支？

```powershell
git branch -d 分支名  # 删除本地分支
git push origin --delete 分支名  # 删除远程分支
```

## 📌 推荐方案

**如果你想要：**
- **保留原版本，继续开发** → 使用方法 1（创建新分支）
- **标记发布版本** → 使用方法 2（创建标签）
- **完全独立的仓库** → 使用方法 3（创建新仓库）

## 🎯 快速命令（假设已安装 Git）

```powershell
# 进入项目目录
cd "C:\Users\32353\Desktop\大四上\NLP\CW\Hotel-Feedback-ABSA-Task-main"

# 初始化仓库（如果还没有）
git init

# 添加远程仓库（替换为你的地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 添加所有文件
git add .

# 提交
git commit -m "版本 2.0: 修复编码问题和登录功能，优化情感分析"

# 创建并切换到新分支
git checkout -b v2.0

# 推送到 GitHub
git push -u origin v2.0
```

## ⚠️ 注意事项

1. **不要提交敏感信息**（密码、API 密钥等）
2. **检查 .gitignore** 确保不会上传大文件（模型文件等）
3. **首次推送可能需要登录 GitHub**

## 🎉 完成！

上传成功后，你可以在 GitHub 上看到新版本！

